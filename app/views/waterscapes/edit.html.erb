<div class="text-center">
    <h1>編集ページ</h1>
</div>
<div class="row">
    <div class="col-sm-6 offset-sm-3">
        <%= form_with(model: @waterscape, local:true) do |f| %>
            <div class="form-group">
                <%= f.label :image,'画像' %>
                <%= f.file_field :image, class: 'form-control' %>
            </div>
            <%= f.submit '更新' %>
        <% end %>
    </div>
</div>